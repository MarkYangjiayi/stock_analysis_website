version: '3.8'

services:
  backend:
    image: quantify-backend:latest
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data

  frontend:
    image: quantify-frontend:latest
    ports:
      - "3000:3000"
    depends_on:
      - backend
